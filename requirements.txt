# Apify SDK for Actor development
apify < 3.0

# OCR and image processing
pytesseract==0.3.10
Pillow==11.0.0  # Updated for Python 3.13 compatibility

# HTTP client - using httpx which is already included with apify
# No need for aiohttp since httpx is async and included
